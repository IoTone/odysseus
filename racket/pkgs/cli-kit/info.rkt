#lang info

;; cli-kit — generic, app-agnostic scaffolding for JSON-emitting CLIs.
;; Standalone Racket package: no Odysseus knowledge, publishable as-is.
(define collection "cli-kit")
(define version "0.1.0")
(define deps '("base"))
(define pkg-desc "Tiny toolkit for JSON-emitting command-line tools")
